using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UISystem
{
    public enum EUIType
    {
        PanelPlayerLoadout = 0,
        PanelPlayerHealthBar = 1,
        PanelPlayerInventory = 2,
        PanelRecentAcquired = 3,
        PanelPlayerBuff = 4,

        PanelChargeModuleBar = 5,
        Component = 6
    }
}